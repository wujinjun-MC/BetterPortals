pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
        maven { url "https://repo.jpenilla.xyz/snapshots/" }
    }
}

rootProject.name = 'BetterPortals'
gradle.ext.versionName = '0.10.0'

include 'bukkit', 'bungee', 'shared', 'api'