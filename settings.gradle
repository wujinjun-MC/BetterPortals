pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
        maven { url "https://repo.jpenilla.xyz/snapshots/" }
    }
}

rootProject.name = 'BetterPortals'

include 'final', 'bukkit', 'bungee', 'shared', 'api'
