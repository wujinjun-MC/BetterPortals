pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
        maven { url "https://repo.jpenilla.xyz/snapshots/" }
        maven { url "https://papermc.io/repo/repository/maven-public/" }
    }
}

rootProject.name = 'BetterPortals'
gradle.ext.versionName = '0.10.1'

include 'bukkit', 'bungee', 'shared', 'api', 'final'